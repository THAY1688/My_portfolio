// js/main.js
const { createApp, ref } = Vue;

createApp({
    setup() {
        return {
            // All data is now hardcoded in the HTML for better SEO and performance
        }
    }
}).mount('#app');